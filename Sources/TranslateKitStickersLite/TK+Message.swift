import Foundation
@_exported import TranslateKitLite

extension TK.Message {
   /// "Sticker added to favorites!" - Confirmation when a sticker is added to favorites
   public static var stickerAddedToFavoritesDot: String {
      String(
         localized: "TK.Message.stickerAddedToFavoritesDot",
         defaultValue: "Sticker added to favorites!",
         bundle: .module,
         comment: "Confirmation when a sticker is added to favorites"
      )
   }

   /// "Sticker pack installed successfully!" - Confirmation when a sticker pack is installed
   public static var stickerPackInstalledSuccessfullyDot: String {
      String(
         localized: "TK.Message.stickerPackInstalledSuccessfullyDot",
         defaultValue: "Sticker pack installed successfully!",
         bundle: .module,
         comment: "Confirmation when a sticker pack is installed"
      )
   }

   /// "Custom sticker created!" - Confirmation when a custom sticker is created
   public static var customStickerCreatedDot: String {
      String(
         localized: "TK.Message.customStickerCreatedDot",
         defaultValue: "Custom sticker created!",
         bundle: .module,
         comment: "Confirmation when a custom sticker is created"
      )
   }

   /// "Sticker sent successfully!" - Confirmation when a sticker is sent
   public static var stickerSentSuccessfullyDot: String {
      String(
         localized: "TK.Message.stickerSentSuccessfullyDot",
         defaultValue: "Sticker sent successfully!",
         bundle: .module,
         comment: "Confirmation when a sticker is sent"
      )
   }

   /// "Keyboard access required for stickers." - Message when keyboard access is needed for stickers
   public static var keyboardAccessRequiredForStickersDot: String {
      String(
         localized: "TK.Message.keyboardAccessRequiredForStickersDot",
         defaultValue: "Keyboard access required for stickers.",
         bundle: .module,
         comment: "Message when keyboard access is needed for stickers"
      )
   }

   /// "Sticker pack download complete!" - Confirmation when a sticker pack download finishes
   public static var stickerPackDownloadCompleteDot: String {
      String(
         localized: "TK.Message.stickerPackDownloadCompleteDot",
         defaultValue: "Sticker pack download complete!",
         bundle: .module,
         comment: "Confirmation when a sticker pack download finishes"
      )
   }

   /// "Background removed successfully!" - Confirmation when a sticker's background is removed
   public static var backgroundRemovedSuccessfullyDot: String {
      String(
         localized: "TK.Message.backgroundRemovedSuccessfullyDot",
         defaultValue: "Background removed successfully!",
         bundle: .module,
         comment: "Confirmation when a sticker's background is removed"
      )
   }

   /// "Animation effects applied!" - Confirmation when animation effects are applied to a sticker
   public static var animationEffectsAppliedDot: String {
      String(
         localized: "TK.Message.animationEffectsAppliedDot",
         defaultValue: "Animation effects applied!",
         bundle: .module,
         comment: "Confirmation when animation effects are applied to a sticker"
      )
   }

   /// "Sticker saved to your collection!" - Confirmation when a sticker is saved to a collection
   public static var stickerSavedToYourCollectionDot: String {
      String(
         localized: "TK.Message.stickerSavedToYourCollectionDot",
         defaultValue: "Sticker saved to your collection!",
         bundle: .module,
         comment: "Confirmation when a sticker is saved to a collection"
      )
   }

   /// "Your sticker pack is ready!" - Confirmation when a sticker pack is ready to use
   public static var yourStickerPackIsReadyDot: String {
      String(
         localized: "TK.Message.yourStickerPackIsReadyDot",
         defaultValue: "Your sticker pack is ready!",
         bundle: .module,
         comment: "Confirmation when a sticker pack is ready to use"
      )
   }

   /// "Sticker added to keyboard!" - Confirmation when a sticker is added to the keyboard
   public static var stickerAddedToKeyboardDot: String {
      String(
         localized: "TK.Message.stickerAddedToKeyboardDot",
         defaultValue: "Sticker added to keyboard!",
         bundle: .module,
         comment: "Confirmation when a sticker is added to the keyboard"
      )
   }

   /// "Effect applied successfully!" - Confirmation when an effect is applied to a sticker
   public static var effectAppliedSuccessfullyDot: String {
      String(
         localized: "TK.Message.effectAppliedSuccessfullyDot",
         defaultValue: "Effect applied successfully!",
         bundle: .module,
         comment: "Confirmation when an effect is applied to a sticker"
      )
   }

   /// "Changes saved to sticker!" - Confirmation when changes are saved to a sticker
   public static var changesSavedToStickerDot: String {
      String(
         localized: "TK.Message.changesSavedToStickerDot",
         defaultValue: "Changes saved to sticker!",
         bundle: .module,
         comment: "Confirmation when changes are saved to a sticker"
      )
   }

   /// "Pack shared with friends!" - Confirmation when a sticker pack is shared with friends
   public static var packSharedWithFriendsDot: String {
      String(
         localized: "TK.Message.packSharedWithFriendsDot",
         defaultValue: "Pack shared with friends!",
         bundle: .module,
         comment: "Confirmation when a sticker pack is shared with friends"
      )
   }

   /// "Pack imported successfully!" - Confirmation when a sticker pack is imported
   public static var packImportedSuccessfullyDot: String {
      String(
         localized: "TK.Message.packImportedSuccessfullyDot",
         defaultValue: "Pack imported successfully!",
         bundle: .module,
         comment: "Confirmation when a sticker pack is imported"
      )
   }
}
